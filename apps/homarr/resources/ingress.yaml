---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homarr
  annotations:
    kubernetes.io/ingress.class: "nginx"
    ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: "nginx"
  rules:
    - host: homarr.internal
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: homarr
              port:
                name: "http"
